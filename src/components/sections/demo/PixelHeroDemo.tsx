"use client";

import { PixelHero } from "@/components/ui/pixel-hero";

export default function PixelHeroDemo() {
  return (
    <div className="dark bg-background text-foreground flex min-h-screen flex-col">
      <PixelHero />
    </div>
  );
}
